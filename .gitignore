# Dependencies
node_modules/

# Environment variables (contains passwords & API keys — never commit)
backend/.env

# Database dumps & scripts with embedded credentials — never commit
backend/database/*.sql
backend/database/*.bat

# Vite build output & cache
frontend/dist/
frontend/.vite/
frontend/node_modules/.vite/

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
*.log

# Claude agent internals / scratch outputs
.claude/
outputs/

# Generated report deliverables — useful locally, not source code
/SurveySmart_CodeReview_Fixes.pdf
/SurveySmart_Test_Results.pdf
